# dragtarget

- Python classes: `['DragTarget']`
- Source files: `['drag_target.py']`
- Isolated/service-like: `false`

## Widget/Control Type
- Protocol control type (`_get_control_name`): `dragtarget`

## Protocol Shape
```json
{
  "_c": "dragtarget",
  "attrs": [
    "group"
  ],
  "events": {
    "api": [
      "on_accept",
      "on_leave",
      "on_will_accept"
    ],
    "runtime": [
      "accept",
      "leave",
      "will_accept"
    ]
  },
  "invokes": []
}
```

## Serialized Attribute Keys
- `group`

## Event Handlers (Python API fields)
- `on_accept`
- `on_leave`
- `on_will_accept`

## Runtime Event Names (wire event names)
- `accept`
- `leave`
- `will_accept`

## Public API Methods
- `content()`
- `content(value)`
- `group()`
- `group(value)`
- `on_accept()`
- `on_accept(handler)`
- `on_leave()`
- `on_leave(handler)`
- `on_will_accept()`
- `on_will_accept(handler)`

## Invoke-Method Protocol Calls
- _No explicit `invoke_method` calls detected in this component._

## Event Payload Shapes (ControlEvent models)
- `DragTargetAcceptEvent` fields: `['src_id', 'x', 'y']` (defined in `drag_target.py`)

## Notes
- Generated by static AST analysis of Flet Python source.
- Attribute and event lists are code-derived; runtime dynamic behavior may add additional values.
