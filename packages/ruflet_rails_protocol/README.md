# Ruflet Rails Protocol

`ruflet_rails_protocol` provides the Rails/Rack WebSocket bridge used by `ruflet_rails`.

It is an internal transport package and is expected to be used through `ruflet_rails`.
