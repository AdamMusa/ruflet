/* Digest module for the embedded VM. The implementation is shared with the
 * platform plugins so host test builds and device builds stay identical. */
#include "../../../../shared/mruby_digest_gem.c"
